# litewebui — copy to .env (never commit .env)

LISTEN=:3050
DATA_DIR=./data

AUTH_USERNAME=admin
# SHA-256("changeme") — change before production:
#   echo -n 'yourpass' | sha256sum | awk '{print $1}'
AUTH_PASSWORD=057ba03d6c44104863dc7361fe4578965d1887360f90a0895882e58a6248fc86

NINEROUTER_BASE_URL=http://127.0.0.1:20128/v1
NINEROUTER_API_KEY=
